<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Activity Logs</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-800 text-white">
    <div class="min-h-screen p-10">
        <h1 class="text-3xl font-bold mb-6 text-center text-blue-400">Activity Logs</h1>
        
        <!-- Back Button -->
        <div class="mb-4">
            <a href="/adminResource" class="inline-block px-6 py-2 bg-gray-600 text-white rounded-md font-medium hover:bg-gray-700 transition-all">
                ← Back to Dashboard
            </a>
        </div>

        <!-- Activity Log Table -->
        <table class="table-auto w-full text-left bg-gray-700 text-white rounded-lg shadow-md">
            <thead class="bg-gray-600">
                <tr>
                    <th class="px-4 py-2">Username</th>
                    <th class="px-4 py-2">Role</th>
                    <th class="px-4 py-2">Action</th>
                    <th class="px-4 py-2">Timestamp</th>
                </tr>
            </thead>
            <tbody>
                <% logs.forEach(log => { %>
                    <tr class="border-t border-gray-600">
                        <td class="px-4 py-2"><%= log.username %></td>
                        <td class="px-4 py-2"><%= log.role %></td>
                        <td class="px-4 py-2"><%= log.action %></td>
                        <td class="px-4 py-2"><%= new Date(log.timestamp).toLocaleString() %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>
