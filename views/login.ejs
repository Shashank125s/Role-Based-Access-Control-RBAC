<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-indigo-500 to-blue-600">

    <div class="w-full h-screen flex justify-center items-center bg-cover bg-center p-6">
        <div class="bg-zinc-800 p-8 rounded-lg shadow-lg w-full max-w-md">
            <h3 class="text-4xl text-center text-white mb-6">Login to Your Account</h3>

            <% if (error) { %>
                <p class="text-red-500 text-center mb-4"><%= error %></p>
            <% } %>
            
            <form method="post" action="/login" class="space-y-6">
                <!-- Username Input -->
                <div class="flex flex-col">
                    <label for="username" class="text-white mb-2">Username</label>
                    <input class="px-4 py-2 rounded-md bg-transparent border-2 border-zinc-700 text-white outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500" 
                        type="text" name="username" placeholder="Enter Username" required>
                </div>

                <!-- Password Input -->
                <div class="flex flex-col">
                    <label for="password" class="text-white mb-2">Password</label>
                    <input class="px-4 py-2 rounded-md bg-transparent border-2 border-zinc-700 text-white outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500" 
                        type="password" name="password" placeholder="Enter Password" required>
                </div>

                <!-- Login Button -->
                <div class="flex justify-center">
                    <button class="px-4 py-2 rounded-md bg-blue-600 text-white cursor-pointer hover:bg-blue-700 transition-all duration-300 w-full">
                        Login
                    </button>
                </div>
            </form>

            <div class="text-center mt-6">
                <p class="text-white">Don't have an account? <a href="/register" class="text-blue-400 hover:underline">Register here</a></p>
            </div>
        </div>
    </div>

</body>
</html>
